# AGENTS.md

AI agent 在本仓库中工作的行为准则。人类贡献者也适用同样的约定。

## 项目背景

einox 是通用 agent 基座库——三层栈 **eino（LLM 应用框架）→ einox（agent 运行时基座）→ 业务 agent（应用）** 中间的机制层。本仓只做**机制**（循环引擎、会话、审批、沙箱、harness、工具族），不承载**业务内容**（业务工具、提示词内容、审批名单、UI）。定位与能力面详见 [docs/](docs/01-positioning.md)。

## 常用命令

- 构建：`go build ./...`
- 测试：`go test ./...`（全绿才提交；沙箱包按平台构建标签分流——darwin 走 Seatbelt、linux 需要 Landlock 内核，非目标平台自动跳过）
- 测试不碰真实模型端点：`llmtest` 假模型注入 `Options.NewModel`
- 提交前：`go build ./... && go test ./...`

## 架构红线

- `contract/` 零 eino import、全树禁 import 业务模块——两道守卫在 [boundary_test.go](boundary_test.go)，破坏即测试失败
- **机制归基座、内容归业务**：不往本仓添加业务工具、业务提示词内容、审批名单、展示逻辑
- **实现序**：先挖尽 eino 既有能力（中间件/原语/预置拓扑），确无才自研，并在注释或 PR 描述说明「eino 无 X」依据
- **新能力一律做成 `engine.Options` 可选项**：nil 即不装配、零行为变化；不给能力引入 Options 以外的启停方式

## 溯源纪律（版权红线）

- 开源代码的**表达级搬运**（移植 / 直译 / 测试用例改编 / 提示词文本适配）必须：① 文件头注释注明来源与协议；② [NOTICE.md](NOTICE.md) 登记
- 闭源源（如 Claude Code 等产品）只许参考行为语义，**禁止拷贝任何文本、代码或资产**
- 新增依赖仅接受宽松协议（MIT / Apache-2.0 / BSD）；GPL / AGPL / MPL 一律不进

## 编码约定

- 注释与文档用中文，标识符保持英文；目录名从简（单词，避免复合词）
- 外科手术式修改：只改必须改的内容，不顺手重构无关代码
- 简单优先：只写解决问题所需的最少代码，不做超出需求的扩展与抽象
- 有更简单做法或有困惑，直接在 PR/issue 里说，不默默选择
- 编码风格原则（简单优先 / 外科手术式修改等）转述自业界通行的 agent 协作建议（源自 Andrej Karpathy 的公开分享）
